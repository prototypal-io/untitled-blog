<nav class="nav">
  <div class="menu-bar">
    <div class="menu-bar__section">
      <div class="menu-bar__brand">
        {{link-to "Untitled Blog" "posts"}}
      </div>
    </div>

    <div class="menu-bar__section menu-bar__section--right">
      <div class="menu-bar__item">
        {{link-to "Home" "posts"}}
      </div>

      <div class="menu-bar__item">
        {{link-to "About" "posts"}}
      </div>

      <div class="menu-bar__item">
        {{link-to "Sample Post" "posts"}}
      </div>

      <div class="menu-bar__item">
        {{link-to "Contact" "posts"}}
      </div>
    </div>
  </div>
</nav>

<main>
  {{outlet}}
</main>

<footer class="footer">
  <div class="footer__social-links">
    <a href="#" class="footer__social-link">
      {{#fa-stack size="lg" as |s|}}
        {{s.stack-2x "circle"}}
        {{s.stack-1x "twitter" inverse=true}}
      {{/fa-stack}}
    </a>

    <a href="#" class="footer__social-link">
      {{#fa-stack size="lg" as |s|}}
        {{s.stack-2x "circle"}}
        {{s.stack-1x "facebook" inverse=true}}
      {{/fa-stack}}
    </a>

    <a href="#" class="footer__social-link">
      {{#fa-stack size="lg" as |s|}}
        {{s.stack-2x "circle"}}
        {{s.stack-1x "github" inverse=true}}
      {{/fa-stack}}
    </a>
  </div>

  <p class="footer__copyright">Copyright © Your Website 2016</p>
</footer>
